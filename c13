#include<stdio.h>
void main()
{ 
  int a,b,c;
  printf("Enetr 3 ages : ");
  scanf("%d%d%d", &a, &b, &c);
 if(a<b)
  if(a<c)
  printf("a is smaller\n",a);
  else 
 if (b>c)
 printf("b is grater\n",b);
 else 
 printf("c is smaller");
}
